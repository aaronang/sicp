; Since Scheme uses as applicative-order evaluation, the operands will be
; evaluated before applying the procedure to the resulting arguments. This
; program will never end because the else clause will always be evaluated.